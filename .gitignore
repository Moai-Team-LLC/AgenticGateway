# deps
node_modules/

# runtime state (control-plane store, evidence, caches)
/data/
bifrost/data/*
!bifrost/data/config.json

# env & secrets — never commit
.env
.env.*
!.env.example

# editor/os
.DS_Store
*.log
